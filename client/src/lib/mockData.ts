// This function is no longer needed since articles are initialized directly in storage
export async function initializeMockData() {
  // Data is now initialized automatically when storage is created
  console.log("Sample news data loaded automatically");
}
